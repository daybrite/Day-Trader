/target
/build
Cargo.lock
/.cargo/config.toml

# Gradle local state. platform/android/ is source, but everything Gradle generates inside it is
# not: the caches, the per-module build/ trees, and the SDK path local.properties records. These
# need their own entries because `/build` above is anchored to the repo root and so never matches
# platform/android/build/, which is what fails the pristine check the android-mdc job runs
# before packing (an untracked file is an input the commit does not describe).
platform/android/.gradle/
platform/android/**/build/
platform/android/local.properties

# The website is generated: the daysite template clone (local preview) and the data files the
# pipeline synthesizes from Day.toml + store/ + release assets. Only site.toml and theme.css
# are source.
website/.daysite/
website/appindex.json
website/channels.json
website/gallery-manifest.json
# The same pair again under each extra build channel's own directory (website/main/).
website/*/appindex.json
website/*/gallery-manifest.json

# HarmonyOS reads its icon media from a fixed place under each module, so `day prepare`
# links these two directories to the generated set under build/day/host/harmony/media.
/platform/harmony/entry/src/main/resources/base/media
/platform/harmony/AppScope/resources/base/media

# Per-checkout Xcode settings: each platform/<p>/DayApp.xcconfig ends with an optional
# `#include? "DayApp.local.xcconfig"`, so a developer can set their signing team (and anything
# else this machine needs) without committing it. Absent on a fresh clone and in CI, which is
# why the include is optional.
platform/*/DayApp.local.xcconfig
